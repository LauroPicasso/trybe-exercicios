const numbers = [18, 19, 23, 53, 4, 5, 76, 23, 54];

function sumPair() {
  // const result = (pair, number) => ((number % 2 === 0) ? number : pair);
  //  return numbers.reduce(result, 0);
  // return numbers.filter((number) => number % 2 === 0).reduce((result, number) => result + number);
} 

console.log(sumPair());